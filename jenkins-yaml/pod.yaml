---
apiVersion: v1
kind: Pod
metadata:
  labels:
    some-label: some-label-value
spec:
  containers:
    - name: maven
      image: 'maven:3.8.6-openjdk-11-slim'
      command:
            - cat
      tty: true
    - name: docker
      image: illya/aws-cli
      command:
            - cat
      securityContext:
        privileged: true
      tty: true
      env:
        - name: DOCKER_TLS_CERTDIR
          value: ""
    - name: gitleaks
      image: 'zricethezav/gitleaks'
      command:
            - cat
      tty: true
    - name: dockerfile-sec
      image: 'cr0hn/dockerfile-sec'
      command:
            - cat
      tty: true
  nodeSelector:
    eks.amazonaws.com/nodegroup: Worker-Node-Group
